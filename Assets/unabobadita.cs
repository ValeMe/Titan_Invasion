using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class unabobadita : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       int luna=7;
       bool blanco;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
